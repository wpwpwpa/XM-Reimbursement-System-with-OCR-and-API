# 报销截图智能重命名工具 — 产品需求文档 (PRD)

> 版本: v1.0 | 状态: 待评审 | 日期: 2026-07-09

---

## 1. 项目背景与目标

### 1.1 问题

员工日常报销时需收集各电商平台购物截图（淘宝、美团、拼多多等），手动按 `{价格}{平台}` 格式重命名文件，效率低、易出错。

### 1.2 目标

开发一款 **桌面 GUI 工具**，用户选择文件夹后自动：
1. OCR 识别每张截图中的订单信息
2. 提取「实付金额」和「所属平台」
3. 按规则批量重命名：`{金额}{平台}{原扩展名}`（例：原图 `QQ_1783556795637.png` → `26.52淘宝.png`）

### 1.3 范围（第一期）

| 包含 | 不包含 |
|------|--------|
| 淘宝 / 天猫 订单截图识别 | 发票文件处理 |
| 美团（外卖/买菜）订单截图识别 | 京东 / 抖音 / 饿了么 等其他平台 |
| 拼多多 订单截图识别 | 商品详情提取（只提取金额+平台）|
| 识别失败跳过 + 日志 | — |
| 预览后逐行手动修改新文件名 | — |

---

## 2. 用户故事

### 主要用户流程

```
1. 用户打开工具 → 看到"选择文件夹"按钮
2. 点击按钮 → 弹出系统文件夹选择器
3. 选择包含订单截图的文件夹 → 确认
4. 工具扫描文件夹内所有图片文件（jpg/png）
5. 逐张调用 OCR → 解析金额 + 平台
6. 展示预览列表（原文件名 → 新文件名）
7. 用户点击"执行重命名"→ 批量完成
8. 生成处理日志（成功/失败明细）
```

### 典型输入示例

| 原始文件名 | 识别结果 | 目标文件名 |
|-----------|---------|-----------|
| `QQ_1783556795637.png` | ¥26.52 / 淘宝 | `26.52淘宝.png` |
| `IMG_20260708_0046.jpg` | ¥14.6 / 美团 | `14.6美团.jpg` |
| `微信图片_202607090038.jpg` | ¥14.1 / 拼多多 | `14.1拼多多.png` |

---

## 3. 功能需求

### 3.1 核心功能

#### F1 — 文件夹选择
- [F1.1] 支持系统原生文件夹选择对话框
- [F1.2] 选择后显示路径及图片数量统计
- [F1.3] 仅扫描 `.jpg` / `.jpeg` / `.png` 文件

#### F2 — OCR 识别引擎
- [F2.1] 通过统一 OcrBackend 接口调用当前选中的识别模式（A/B/C）对每张截图进行识别
- [F2.2] 输出结构化 JSON：`{ "amount": float, "platform": string, "confidence": float }`
- [F2.3] 金额取「实付款 / 实付 / 实付款」字段，非商品单价
- [F2.4] 平台识别映射表：

| 特征词 | 平台名 | 文件名后缀 |
|--------|--------|-----------|
| 天猫 / 淘宝 / 企 | 淘宝 | `淘宝` |
| 闪购 | 美团 | `美团` |
| 拼小圈 | 拼多多 | `拼多多` |

> 平台判定依据截图上的**必存字段**，不用模糊词：
> - 淘宝：天猫/淘宝/`企`（如"企业购"）是淘宝系订单截图必定存在的字段，命中其一即判淘宝
> - 美团：通过 `闪购` 判定
> - 拼多多：通过 `拼小圈` 判定；**禁用 `拼多多驿站`**（那是代收驿站，非购物平台，会误判）

> 新文件名保留原始扩展名（.jpg/.png 等），仅替换文件名主体，避免改扩展名导致文件无法打开。

- [F2.5] confidence < 阈值（如 0.7）视为识别失败

#### F3 — 重命名预览与执行
- [F3.1] 识别完成后展示表格预览：
  - 列：原文件名 | 识别金额 | 识别平台 | 新文件名 | 状态
  - 状态色标：✅ 成功（绿）/ ⚠️ 低置信度（黄）/ ❌ 失败（红）
- [F3.2] 支持逐行手动修改新文件名（双击编辑）
- [F3.3] 「全部执行」按钮触发批量重命名
- [F3.4] 同一目标文件名冲突时自动追加序号：`26.52淘宝(1).jpg`

#### F4 — 处理日志
- [F4.1] 执行后生成日志文件，保存在目标文件夹内：`rename_log_{时间戳}.json`
- [F4.2] 日志内容：

```json
[
  {
    "original_name": "QQ_1783556795637.png",
    "new_name": "26.52淘宝.jpg",
    "status": "success",
    "amount": 26.52,
    "platform": "淘宝",
    "confidence": 0.95,
    "timestamp": "2026-07-09T08:30:00"
  },
  {
    "original_name": "blurry_photo.jpg",
    "new_name": null,
    "status": "failed",
    "reason": "无法识别订单信息",
    "confidence": 0.3,
    "timestamp": "2026-07-09T08:30:01"
  }
]
```

### 3.2 边界情况处理

| 场景 | 处理方式 |
|------|---------|
| 截图模糊/无法识别 | 标记失败，跳过，记录日志 |
| 一张图含多个订单 | 取「实付款」总额作为金额 |
| 价格有优惠抵扣 | 取最终实付款（扣除优惠后） |
| 文件夹为空 / 无图片 | 提示用户，不执行 |
| 目标文件已存在 | 追加序号 (1), (2)... |
| 识别金额但无法判断平台 | 标记失败（平台是必须字段） |
| 识别平台但无法确定金额 | 标记失败（金额是必须字段） |

---

## 4. 非功能需求

### 4.1 性能
- 单张图片 OCR + 解析耗时（取决于所选模式：云端视觉最快，本地 GGUF 较慢；模式 C 以稳定完成为主，不作硬性 5 秒约束）
- 支持 50 张以内图片的文件夹流畅处理
- 进度条实时显示当前处理第几张/共几张

### 4.2 可靠性
- 重命名操作支持**撤销**（一键恢复原始文件名）
- 日志持久化，可回溯每次操作历史
- 断网时提示错误，不执行重命名

### 4.3 易用性
- 界面简洁，≤ 3 步完成全流程（选文件夹 → 预览 → 执行）
- 默认模式 C（本地 OCR+LLM）开箱即用免配置；云端/本地视觉模式需用户自行填写连接参数

---

## 5. 技术方案

### 5.1 技术栈（重写决策）

> 参考项目复盘：`报销系统`、`ReimburseStandalone` 均用 **tkinter + RapidOCR + 正则抽取**，痛点是：tkinter 丑、正则脆（跨行/版本差异易错）、GUI 与引擎强耦合。本次重写采用现代 GUI 框架，OCR 抽象为三模式可切换后端。

| 组件 | 选型 | 说明 |
|------|------|------|
| GUI 框架 | **PyQt6（优先）** / Electron+Vue（备选） | 取代 tkinter；PyQt6 与 RapidOCR 同进程零桥接 |
| OCR 抽象层 | `OcrBackend` 接口统一封装 | 三模式共用同一套结果解析逻辑 |
| 结构化解析 | 大模型 / LLM / 正则 三选一 | 见 5.2 |
| 语言 | Python（主） | 与 RapidOCR/torch 同生态，单进程直调 |

### 5.1.1 GUI 框架选型对比

| 维度 | Electron + Vue | PyQt6 |
|------|---------------|-------|
| 技术栈 | HTML/CSS/JS（前端生态） | Python 原生控件 |
| 打包体积 | 大（80–150MB，含 Chromium） | 小（15–40MB） |
| 与 RapidOCR 集成 | 需 Python 子进程或 wasm 桥接 | 同一进程直接调用，零桥接 |
| UI 美观度 | 高，CSS 自由度高 | 中，需手动调样式（QSS） |
| 跨平台一致性 | 极好 | 较好（偶有原生差异） |
| 开发效率 | 前端组件丰富，迭代快 | Python 直写，调试简单 |
| 推荐场景 | 重视觉、需复杂交互动效 | 重视轻量、与 Python OCR 生态紧耦合 |

> 本工具 OCR 计算重、与 Python 生态（RapidOCR / torch）天然贴合，**优先推荐 PyQt6**；若后续追求极致 UI 动效再考虑 Electron。

### 5.2 三种 OCR 识别模式（核心架构）

全部模式输出统一结构：`{ amount: float, platform: string, confidence: float }`，上层逻辑无感知。

#### 模式 A：云端视觉大模型 API
- 直接传图片给视觉大模型（GPT-4V / 通义 / 豆包 / 混元等）
- 模型输出结构化 JSON（金额 + 平台）；**confidence 由客户端合成**（视觉 API 不返回数值置信度）
- 优点：精度最高，免本地算力；缺点：需联网、有 API 成本
- 适用：追求识别率、有网络环境

**confidence 合成（与模式 C 共用默认阈值 0.70）**
视觉 API 无原生置信度，按结果结构合成：
- base 0.5：成功返回结构化 JSON
- +0.3：字段完整（amount + platform 均存在）
- +0.1：金额合法且合理（正数、合理区间、非商品单价）
- +0.1：platform 归一化后在白名单 {淘宝, 美团, 拼多多} 内
- 缺 amount 或缺 platform → 合成分 < 0.70，标记失败（与模式 C 失败逻辑一致）
- 不采用模型自报置信度（不可靠，M1 验证后再评估）

#### 模式 B：本地视觉模型
- 本机部署视觉模型（Ollama / LM Studio 加载视觉权重）
- 离线可用，数据不出本机
- 优点：隐私好、零 API 费；缺点：需 GPU 或较大内存、模型体积大
- 适用：无网环境、敏感数据

#### 模式 C：OCR + 文字处理模型（复用 RapidOCR）
- 阶段 1：RapidOCR（`rapidocr-onnxruntime`，CPU 可跑）提取纯文本
- 阶段 2：本地或云端 LLM 解析文本 → 金额 + 平台
- 平台判定仍走正则（参考旧项目经验：模型易误判平台，正则更稳）
- 优点：成本低、兼容现有设备、RapidOCR 已验证可用；缺点：两阶段误差累积
- 适用：默认兜底模式、低配机器

```
                    ┌─────────────────────────────┐
                    │       OcrBackend 抽象层      │
                    └─────────────────────────────┘
                       │          │           │
            ┌──────────┘          │           └──────────┐
            ▼                     ▼                      ▼
      [模式A 云端视觉API]    [模式B 本地视觉模型]   [模式C OCR+文字模型]
      图片→JSON             图片→JSON          图片→RapidOCR→文本→LLM→JSON
            │                     │                      │
            └──────────┬──────────┴──────────────────────┘
                       ▼
                { amount, platform, confidence }
```

### 5.3 重复使用资产
- `RapidOCR` 调用方式直接复用旧项目 `RapidOCR(use_angle_cls=True, use_gpu=False)`，结果取 `item[1]` 拼文本
- 平台映射正则（淘宝/天猫、美团、拼多多）可直接迁移
- LLM 三后端（进程内 GGUF / 本地 API / 云端 API）抽象设计可参考 `model_backend.py`

### 5.4 模式 C 详细设计（第一期实现目标）

> 第一期优先实现模式 C：复用 RapidOCR（已验证）+ LLM 解析，成本最低、最快出可用版。模式 A / B 预留接口，后续接入。

#### 5.4.1 数据流
```
图片 → RapidOCR → full_text(纯文本)
                     │
        ┌────────────┴─────────────┐
        │ 阶段2a：正则抽取（兜底）   │ 阶段2b：LLM 解析（增强）
        │ extract_price/platform    │ 本地GGUF / 本地API / 云端API
        │ 直接出金额+平台            │ 文本→JSON→金额+平台
        └────────────┬─────────────┘
                     ▼
           平台判定：强制走正则（模型易误判，不用）
           金额/日期：正则优先，LLM 非空则覆盖
                     ▼
              { amount, platform, confidence }
```

#### 5.4.2 阶段1 — RapidOCR 调用
```python
from rapidocr_onnxruntime import RapidOCR
engine = RapidOCR(use_angle_cls=True, use_gpu=False)
result, _ = engine(str(image_path))
# result: [(box, text, score), ...] 取 item[1] 拼接
full_text = "\n".join(i[1].strip() for i in result if i and len(i) >= 2 and i[1])
```

#### 5.4.3 阶段2 — 平台判定（正则，强制，不交给 LLM）
命中即定，不再向下；特征词独立，互不覆盖：

1. （可选辅助）父文件夹名命中 淘宝/美团/拼多多 → 直接定
2. 截图文本特征词命中（主判定依据）：

| 平台 | 特征词 | 依据 |
|------|--------|------|
| 淘宝 | `天猫` / `淘宝` / `企` | 淘宝/天猫订单截图**必存在**字段（如"企业购"），命中其一即判淘宝 |
| 美团 | `闪购` | 美团订单截图特征词 |
| 拼多多 | `拼小圈` | 拼多多订单截图特征词 |

3. 兜底：所有特征词均未命中 → 标记失败（平台为必须字段，见 3.2 边界情况）

**禁用与注意：**
- ❌ 不得用 `拼多多驿站` 判拼多多 —— 那是代收驿站，非购物平台，会误判
- ❌ 拼多多**不**靠直接词 `拼多多` 判定（截图常含快递/驿站名导致混淆）

#### 5.4.4 阶段2 — 金额抽取（正则 + LLM 增强）
- 正则关键词（平台差异化）：`实付款` / `实付` / `合计` / `实付总额`
- 跨行匹配：OCR 常把「实付款¥107.52」拆成两行，需跨行拼接
- LLM 提示词（仅补金额/日期，不判平台）：
```
你是报销截图解析器。从下方OCR文本中提取「实付金额」（扣除优惠后的最终付款）。
只输出JSON：{"amount": 数值, "date": "YYYY-MM-DD或空"}。
不要推断平台。金额缺失则 amount 为 null。
--- 文本 ---
{full_text}
```

#### 5.4.5 LLM 后端（三选一，复用旧项目 `model_backend.py` 抽象）
| 后端 | 实现 | 联网 |
|------|------|------|
| 进程内 GGUF | torch+transformers 加载 qwen2.5-3b GGUF，懒加载 | 否 |
| 本地 API | urllib 调 `localhost:1234/v1`（Ollama/LM Studio） | 否（本机）|
| 云端 API | urllib 调 `/chat/completions`（DeepSeek 等），弹隐私确认 | 是 |
- 统一接口：`load()` + `extract(text, prompt) → dict`
- 后端连不上 → 自动回退纯正则，不阻断流程
- JSON 解析失败 → 正则 `\{.*\}` 抠出对象兜底

#### 5.4.6 输出契约（三种模式统一）
```json
{ "amount": 26.52, "platform": "淘宝", "confidence": 0.95 }
```
- confidence < 设置阈值（默认 0.70）→ 标记失败，跳过重命名

---

## 6. UI 界面布局（参考 FileNeatAI 风格）

### 6.1 整体框架
- 左侧固定选项卡栏：**文件处理** / **模型选择** / **设置**
- 顶部步骤指示条：`选文件夹 → 识别 → 预览 → 执行`
- 首页（文件处理页）展示全部内容，结果表格分页（每页 10 条）

### 6.2 文件处理页（首页）
```
┌──────────┬──────────────────────────────────────────────┐
│ 报销整理  │  文件处理            选文件夹 › 识别 › 预览 › 执行│
│          ├──────────────────────────────────────────────┤
│ ●文件处理 │  [已扫描 24] [已识别 22] [失败 2]              │
│  模型选择 │  ┌────────────────────────────────────────┐  │
│  设置     │  │  拖入文件夹 或 点击「选择文件夹」        │  │
│          │  └────────────────────────────────────────┘  │
│          │  [选择文件夹][开始识别][执行重命名][撤销]      │
│          │  原文件名 │ 金额 │ 平台 │ 新文件名 │ 状态     │
│          │  QQ_...  │¥26.52│ 淘宝 │26.52淘宝│ ✅成功   │
│          │  IMG_..  │¥14.6 │ 美团 │14.6美团 │ ✅成功   │
│          │  ...     │ ...  │ ...  │ ...     │ ...      │
│          │  共24条 · 第 1/3 页 [上一页][下一页]          │
└──────────┴──────────────────────────────────────────────┘
```

### 6.3 模型选择页（单选卡片 + 配置内联展开）

- 三个单选卡片选模式；**选中的卡片自身内联展开配置表单**（非卡片下方独立区），未选中卡片仅显示标题+说明
- 配置项写在对应卡片内，写入后保存到 `config.json`，全局生效

**① 云端视觉大模型 API（展开配置）**
```
API 协议：[下拉: OpenAI 兼容 / 通义千问 / 豆包 / 混元]
API 端点：[text: 随协议预填默认地址，可改]
API Key：[password 输入框]
模型名：[text: 如 gpt-4o / qwen-vl-max]
```

**② 本地视觉模型（展开配置）**
```
服务地址：[text: http://localhost:11434/v1]
模型名：[text: 如 llama3.2-vision]
```

**③ OCR + 文字模型（默认，展开配置）**
```
RapidOCR 开关：[toggle: 开]
LLM 后端：[下拉: 进程内GGUF / 本地API / 云端API]
  ├─ 进程内GGUF: 模型路径 [text: ./models/qwen2.5-3b.gguf]
  ├─ 本地API:    端点 [text: http://localhost:1234/v1] 模型名 [text]
  └─ 云端API:    API Key [password] 端点 [text] 模型名 [text]
[测试连接] 按钮 → 返回连通状态
```

> 模型选择页 = 选择 + 写入配置，非纯单选。每张卡片选中即展开其表单。

### 6.4 设置页
- 输出格式模板：`{price}{platform}{ext}`（保留原扩展名，可改）
- 文件名冲突策略：追加序号 `(1)(2)...`
- 低置信度阈值：0.70
- 识别失败处理：跳过并记录日志
- 日志保存路径：默认与目标文件夹同目录 `rename_log_{时间戳}.json`（可在设置页修改）
- 支持文件类型：`.jpg .jpeg .png`

### 6.5 交互状态机

```
[初始态] → 选择文件夹 → [已选文件夹]
                              ↓                点击"开始识别"
                         [识别中] ←←←←←←←←←←←←┘
                           ↓ (全部完成)
                       [识别完成] → 用户翻页检查预览
                                        ↓           点击"执行重命名"
                                   [重命名中]
                                        ↓ (全部完成)
                                       [完成态]
                                            ↑
                                    点击"撤销" ──┘
```

---

## 7. 数据流

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 文件夹    │───▶│ 文件扫描  │───▶│ OCR 识别  │───▶│ 结果解析  │
│ (输入)    │    │ (过滤图片)│    │ (按所选模式)│  │ (JSON)   │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
┌──────────┐    ┌──────────┐    ┌──────────┐            │
│ 日志文件  │◀───│ 批量重命名│◀───│ 用户确认  │◀───────────┘
│ (.json)   │    │ (fs 操作)│    │ (预览/编辑)│
└──────────┘    └──────────┘    └──────────┘
```

---

## 8. 验收标准 (DoD)

### 必须通过（P0）

- [ ] 能正确识别淘宝/天猫订单截图中的实付金额和平台名
- [ ] 能正确识别美团订单截图中的实付金额和平台名
- [ ] 能正确识别拼多多订单截图中的实付金额和平台名
- [ ] 重命名格式符合 `{金额}{平台}{原扩展名}` 规则
- [ ] 识别失败的文件被跳过且记录到日志
- [ ] 文件名冲突时自动追加序号
- [ ] 支持撤销操作恢复原名

### 应该通过（P1）

- [ ] 处理 20 张截图总耗时达标（云端视觉/本地 API 模式 ≤ 60 秒；本地 GGUF 模式可适当放宽，以稳定完成为准）
- [ ] UI 进度反馈准确
- [ ] 低置信度结果有黄色警告标识

### 可以通过（P2）

- [ ] 支持拖拽文件夹到窗口快速选择
- [ ] 深色模式适配

---

## 9. 里程碑规划

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| M1 | 技术验证（PoC） | OCR 识别 Demo：输入 3 张示例图 → 输出正确 JSON |
| M2 | GUI 主框架 + 文件夹选择/扫描 | 可选择文件夹并展示文件列表 |
| M3 | OCR 集成 + 预览界面 | 完整识别+预览流程跑通 |
| M4 | 重命名执行 + 日志 + 撤销 | 全功能可用 |
| M5 | 异常处理打磨 + 小功能 | 冲突处理、进度条、P1 验收项 |

---

## 10. 开放问题

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| Q1 | OCR 引擎具体用哪个？ | **已决策**：三模式抽象（A/B/C），第一期实现模式 C 先行 | 模式 A/B 仅预留接口 |
| Q2 | 是否需要支持网络图片 URL 输入？ | 仅影响输入方式 | 第一期暂不支持 |
| Q3 | 大并发场景下 API 限流如何处理？ | 影响用户体验 | 加队列 + 间隔请求 |
| Q4 | 是否需要多语言界面？ | 仅影响国际化 | 第一期仅中文 |

---

*本 PRD 经确认后将进入技术方案设计阶段。*
