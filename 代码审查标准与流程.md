# 代码审查标准与流程（XM 报销 OCR 项目）

> 审查不是挑刺，是把一个人的认知变成团队的护栏。
> 本标准**用本仓库真实代码做正/反例**，不套通用模板。
> 制定日期：2026-07-13 ｜ 审查专家：火眼眼（CodeReviewer）

---

## 1. 技术栈基线表（审查时需对照的维度）

| 维度 | 现状 | 审查关注点 |
|---|---|---|
| 语言 / 运行时 | Python 3.13（managed venv），PyInstaller 打包 | 类型注解一致性、3.13 特性使用 |
| GUI | PyQt6（QtCore / QtGui / QtWidgets 仅此三模块） | 禁止 import 被排除的 Qt 子模块（Qt3D/WebEngine/Qml…） |
| 识别引擎 | RapidOCR + onnxruntime（**懒加载**，启动不触碰） | 重型依赖不得模块级 import |
| LLM 兜底 | 标准库 `urllib` 调 OpenAI 兼容 API（零额外依赖） | 网络超时、异常兜底、密钥处理 |
| 结构化数据 | openpyxl（读淘宝 Excel + 写报销导入 Excel） | 模板结构不可破坏、列宽/表头顺序 |
| 发票解析 | PyMuPDF `fitz`（函数内懒加载） | 仅适配结构化文本型 PDF，非扫描图 |
| 测试 | `app/verify*.py`、`m1_poc/test_*.py` 为手工验证脚本 | 非正式单测，尚无 pytest 门禁 |
| 构建/分发 | `XM报销OCR.spec` 打 onedir 文件夹（191MB） | spec 改动需重打包验证 |

---

## 2. 架构分层强制约定

本仓库当前分层（**已基本遵守，需写入强制约束**）：

```
识别领域层   m1_poc/parser.py        → 平台判定 + 金额 + confidence（纯文本，无 IO）
业务领域层   app/order_fields.py     → 时间/商品名抽取 + 黑名单质检（纯函数）
            app/invoice_parser.py   → 发票 PDF 结构化解析
识别编排层   app/recognizer.py       → 门控：OCR+正则前置，模型兜底（失败安全）
外部适配层   app/llm_backend.py      → LLM/视觉后端抽象（urllib）
            app/order_excel.py      → 淘宝 Excel 读 + 价格/商品名匹配
            app/reimbursement_excel.py → 报销导入 Excel 生成
UI / 编排    app/main.py             → MainWindow（GUI + 后台线程 + 配置 + 导出）
```

**强制规则：**

1. **领域逻辑只能在领域层**：金额/平台只能出自 `m1_poc/parser.py`；商品名黑名单/质检只能出自 `app/order_fields.py`。UI 层不得手抄这些规则（见 §5 反例）。
2. **重型依赖禁止模块级 import**：RapidOCR / cv2 / onnxruntime / fitz 必须在函数内懒加载（`invoice_parser.py:173` 已正确示范）。新增识别依赖须遵守。
3. **单一真相源**：同一派生逻辑只在一处实现，调用方引用而非复制（见 §5 `recognize_one` / `chr(64+c)` 反例）。
4. **失败安全边界**：OCR / LLM / 文件解析是外部边界，异常必须兜底为"失败结果"而非抛出崩溃 GUI（`recognizer.py` / `invoice_parser.py` 已示范），但**内层业务代码不得用 `except Exception` 吞错**。
5. **PyQt6 只用 QtCore/QtGui/QtWidgets**：spec 已 `excludes` 其余子模块，新增代码不得 import 被排除模块，否则打包缺失。

---

## 3. 分级 Checklist（🔴 / 🟡 / 💭）

每条带本仓库 `文件:行号` 正反例。审查时逐条核对。

### 🔴 阻断（合并前必须修复）

> 当前仓库**未发现 🔴 阻断项**。下列为定义，供后续 PR 卡点。

- **安全漏洞**：SQL/命令注入、未鉴权外部调用、密钥硬编码进源码。
  - 正例：`llm_backend.py` 用 `urllib` + 参数化 JSON payload，无拼接执行。
  - 反例（未来禁止）：`eval(user_input)`、`os.system(f"convert {path}")`。
- **数据丢失 / 破坏**：重命名/导出覆盖用户原文件且无备份、配置静默清空。
  - 本仓库现状：`main.py` 重命名有撤销机制（`last_renames`），OK。
- **正确性分歧**：同一入口不同路径结果不一致（如两个门控函数对同图给出不同金额）。见 §5 `recognize_one` 分叉风险。
- **主线程阻塞**：OCR/LLM 网络调用放进 UI 线程导致界面卡死。
  - 正例：`main.py` 用 `RecognitionWorker(QThread)` 后台跑（`recognize_image_gated` 在线程内）。新增同步网络调用必须走线程。

### 🟡 建议（应当修复，可不阻塞紧急修复）

- **重复逻辑 / 手抄分叉**（单一真相源违反）
  - 反例：`app/llm_backend.py:71-74` 与 `:131-134` 重复的 `endpoint.endswith("/v1")` URL 拼接 → 抽 `_build_url(endpoint)`。
  - 反例：`app/order_excel.py:177` 与 `app/reimbursement_excel.py:140` 都用 `chr(64 + c)` 设列宽（仅支持 ≤26 列，且重复）→ 抽共享 `set_col_widths(ws, headers, default=22)`，用 `openpyxl.utils.get_column_letter`。
  - 反例：`app/recognizer.py:49` `recognize_one` 与 `:125` `recognize_image_gated` 门控逻辑高度近似、仅 `verify.py` 用前者 → 应让 `recognize_one` 薄包 `recognize_image_gated`，消除双份门控。
- **死代码 / 孤儿函数**
  - 反例：`app/llm_backend.py:126` `parse_with_prompt` 全仓零引用（方案 A 已移除仲裁，函数遗留）→ Grep 确认零引用后删除。
  - 反例：`app/llm_backend.py:103` `_raw_reason` 赋值后从未使用 → 删除这两行。
- **宽泛异常吞错**
  - 反例：全仓 `except Exception` 约 42 处（`main.py:18`、`llm_backend.py:9` 等）。边界失败安全可接受，但应改为具体异常：`OSError`(文件)、`ValueError`/`json.JSONDecodeError`(解析)、`urllib.error.URLError`(网络)、`TimeoutError`。避免把编程错误（如 `TypeError`）一并吞掉。
- **资源未释放**
  - 反例：`app/order_excel.py:61` `load_workbook(read_only=True)` 异常路径不关 `wb` → 用 `with` 或 `try/finally` 保证 `wb.close()`。
- **全局静默警告**
  - 反例：`app/reimbursement_excel.py:27` 模块级 `warnings.filterwarnings("ignore", ...)` 全局屏蔽 UserWarning → 改为在调用处局部处理或显式设置 workbook 默认样式，避免掩盖真实问题。
- **密钥处理**
  - 反例：`llm_backend` 接收 `api_key` 并被 `main` 持久化进 `config.json`（明文，虽 gitignore）。建议：默认不持久化 key，或文档明示本地明文风险。
- **测试/验证缺口**
  - 现状：仅有 `verify*.py` 手工脚本，无 pytest 门禁。建议：把 `verify.py` 的核心断言迁为 `tests/`，CI 跑。
- **架构巨类**
  - 反例：`app/main.py` 单文件 1994 行、单 `MainWindow` 类含 74 个方法（UI 构建 + 业务编排 + 后台 worker + 配置 + 导出混居）→ God class。建议拆分：`RecognitionWorker` 独立模块、导出逻辑归 `reimbursement_excel`/`order_excel`、UI 构建保持。

### 💭 微调（Nice to Have）

- **死变量**：`llm_backend.py:103` `_raw_reason` 删除（见上）。
- **重复块**：`invoice_parser.py:24-37` `_norm_date` 两个 try/except 结构对称重复 → 抽 `_to_ymd(y,mo,d)` helper。
- **文档漂移**：`m1_poc/parser.py:83` docstring「LLM 增强留接口(本期未接)」与实际 `recognizer` 已接 LLM 不符 → 更新注释。
- **开发脚本混入源码树**：`m1_poc/generate_samples.py`、`m1_poc/test_order_feature.py`、`app/verify*.py` 为 POC/验证脚本 → 移入 `tests/` 或 `tools/`，并确认不进打包（spec `datas` 只收 `m1_poc` 顶层包，需核对这些脚本是否被误打入）。
- **魔法数**：`PRICE_TOLERANCE = 5.0`、`DEFAULT_THRESHOLD = 0.70` 已抽常量（好）；其余阈值（如 `abs(w[1]-nw[1])<=4` 像素容差）建议补命名常量。

---

## 4. 审查流程

### 角色与职责

| 角色 | 职责 |
|---|---|
| 作者（你） | 自审 Checklist、写清晰 PR 描述、确保本地构建通过 |
| 审查者（火眼眼 / 同伴） | 按 §3 分级评论、标注级别、不替作者改代码 |
| 合并者 | 确认所有 🔴 关闭、🟡 有结论（修 or  documented 延期） |

### SLA

- 普通 PR：24h 内首评；紧急修复：4h。
- 评论回复：作者 48h 内响应，否则自动提醒。

### 分级处置规则

- 🔴：**阻塞合并**，必须修复后重审。
- 🟡：**不阻塞紧急发布**，但须二选一——`修复` 或 `记为 Backlog 延期项（需决策）`。禁止无结论合入。
- 💭：建议性，作者自行决定。

### 合并门槛（门禁）

1. `py_compile` 全绿（CI 第一步）。
2. `pytest` 通过（待建，见路线图）。
3. `XM报销OCR.spec` 改动须本地重打包 + 启动冒烟测试通过。
4. 无 🔴 未关闭项；🟡 全部有结论。
5. git 安全：`git add` 具体文件，**禁止** `git add -A` / `--no-verify` / `-f`；提交前 `git status` + `git check-ignore` 确认未带入 `config.json` / `logs/` / `dist/` / `.workbuddy/`。

### 工具链建议

- 静态：`ruff`（lint + 基础规则）、`mypy`（类型）、`vulture`（死代码探测，可自动抓 §5 的 `parse_with_prompt`/`_raw_reason`）。
- 测试：`pytest` + 样本 fixtures（订单截图/发票 PDF 各若干，参考 `invoice_parser.py` 已用 15 张验证的做法）。
- 门禁：GitHub Actions / 司内 CI 跑 lint + type + test + `py_compile`。

---

## 5. 现有代码清理 Backlog（首轮排查）

> 状态列：✅ 已修 ｜ ⏸ 延期-需决策 ｜ 🔲 待修

| # | 级别 | 文件:行 | 现象 | 建议动作 | 状态 |
|---|---|---|---|---|---|
| 1 | 🟡 | `llm_backend.py:126` | `parse_with_prompt` 全仓零引用（方案 A 移除仲裁后遗留） | Grep 确认零引用 → 删除 | ✅ |
| 2 | 💭 | `llm_backend.py:103` | `_raw_reason` 赋值后从未使用 | 删两行 | ✅ |
| 3 | 🟡 | `llm_backend.py:71-74` / `:131-134` | URL 拼接重复 | 抽 `_build_url(endpoint)` | ✅ |
| 4 | 🟡 | `recognizer.py:49` vs `:125` | `recognize_one` 与 `recognize_image_gated` 门控逻辑分叉 | `recognize_one` 薄包 `recognize_image_gated` | ✅ |
| 5 | 🟡 | `order_excel.py:177` / `reimbursement_excel.py:140` | `chr(64+c)` 设列宽，重复且 ≤26 列脆弱 | 抽共享 `set_col_widths`，用 `get_column_letter` | ✅ |
| 6 | 🟡 | `order_excel.py:61` | `read_only` workbook 异常路径不关 | `with` / `try-finally` 关 `wb` | ✅ |
| 7 | 🟡 | `reimbursement_excel.py:27` | 模块级全局 `filterwarnings("ignore")` | 局部处理或显式 workbook 样式 | ✅ |
| 8 | 🟡 | `llm_backend` + `main` | `api_key` 明文持久化进 `config.json` | 默认不持久化 key / 文档明示风险 | ⏸ |
| 9 | 🟡 | `main.py` 全文件 | 1994 行单 `MainWindow` 含 74 方法（God class） | 拆 `RecognitionWorker` / 导出层 / UI 构建 | ⏸ |
| 10 | 🟡 | `llm_backend.test_connection` | 仅 `GET /v1/models` 校验端点可达，不校验 model 是否加载 | 解析返回 `id` 列表校验 model 存在（可选发一次真实推理） | ✅ |
| 11 | 🟡 | 全仓 ~42 处 | `except Exception` 宽泛兜底 | 边界改具体异常（OSError/ValueError/URLError/JSONDecodeError） | ⏸ |
| 12 | 💭 | `invoice_parser.py:24-37` | `_norm_date` 两 try/except 对称重复 | 抽 `_to_ymd(y,mo,d)` | ✅ |
| 13 | 💭 | `m1_poc/parser.py:83` | docstring「LLM 未接」与实际不符 | 更新注释 | ✅ |
| 14 | 💭 | `m1_poc/*.py`、`app/verify*.py` | 开发/验证脚本混入源码树 | 移 `tests/` 或 `tools/`，核对不打进打包 | ⏸ |

### 延期项说明（⏸）
- **#8 api_key 明文持久化**：属产品/安全决策。当前保留明文持久化（用户免每次重填 key），仅在代码与文档明示本地明文风险，未擅自改为不持久化以免破坏工作流。
- **#9 God class 拆分**：`main.py` 1994 行单类是大重构，需独立 PR（先抽 `RecognitionWorker` 到独立模块，verify.py 已能校验 worker 事件循环路径）。强行一次性拆分风险高，列入阶段二。
- **#11 42 处 except Exception**：边界失败安全本身可接受（GUI 不崩）。逐处收敛为具体异常（OSError/ValueError/URLError/JSONDecodeError）是持续项，新代码即时遵守。
- **#14 开发/验证脚本迁移**：`app/verify.py`、`m1_poc/*` 为验证/POC 脚本；spec 未将其打进分发包（verify 不被 main 依赖，m1_poc 仅收顶层包），故不影响产物。迁移到 `tests/`/`tools/` 属布局整理，待定。

**本轮结论**：无 🔴 阻断项。代码质量整体良好——领域层分离清晰、`order_fields.py`/`invoice_parser.py` 的领域逻辑与黑名单质检设计周密、失败安全边界到位、单一真相源基本保持。主要欠账在 🟡 可维护性（死代码、重复、God class）与少量健壮性（异常粒度、资源释放）。

**值得表扬的代码**：
- `app/order_fields.py`：商品名抽取策略（标签优先 → 金额锚点兜底）注释完整、黑名单 `_is_bad_product` 防护 OCR 误命中 UI 按钮/店铺名，是教科书级领域逻辑。
- `app/invoice_parser.py`：坐标法区分购销双方 + 价税合计优先取大写后数字，并有「15 张样本验证 100% 吻合」的实证依据。
- `app/recognizer.py`：`recognize_image_gated` 门控策略文档化、懒加载 `m1_poc`、失败返回 `failed` dict 不崩 GUI。

---

## 6. 三阶段落地路线图

### 阶段一 · 止血（1–2 天）
- 修 §5 中 🔴（本轮无）+ 高价值 🟡：`#1` 死代码删除、`#2` 死变量、`#5` 列宽 helper、`#6` 资源释放。
- 接入 `ruff` + `vulture` 到本地，CI 跑 `py_compile`。
- 产出：Backlog 状态列更新。

### 阶段二 · 提质（1–2 周）
- 迁 `verify.py` 核心断言为 `tests/`（pytest + 样本 fixtures）。
- 收敛重复：`#3` URL helper、`#4` 统一门控、`#7` 警告处理、`#10` 连接校验加强。
- `main.py` 启动拆分 `RecognitionWorker`（先拆最独立的，不强求一次性重构）。
- 建 GitHub Actions：lint + type + test + `py_compile` 门禁。

### 阶段三 · 持续
- 每个 PR 走 §4 流程，Backlog 滚动更新。
- 季度回顾：Backlog 清零情况、新增 🔴 趋势。
- 考虑把本流程存为 skill 复用（见收尾）。

---

## 收尾

- 文档 §5 Backlog 状态列随修复推进更新（✅ / ⏸）。
- 一轮结束后建议：是否把本流程存成 skill 复用、是否开 PR、剩余 🟡 是否继续。
